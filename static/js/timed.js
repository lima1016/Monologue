/* 1분 말하기: one question, sixty seconds of speaking, graded after, then once
   more to compare. The pick screen hands a created session here.

   A stub until the screen itself is built: pick.js calls it with
   { sessionId, topic, meaning, starter } once POST /sessions has made a timed
   session. */
export function openTimed(ctx) {}
